spring:
  application.name:
    bonus-service
  webflux:
    base-path: /api/v1
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      enabled: true

server:
  port: ${PORT:8070}
